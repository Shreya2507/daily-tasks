Q7.  Find all .conf files in /etc, sort them alphabetically, and save to conf_list.txt.

shreya-mathur@TTNPL-10419:~$ **sudo find /etc -type f -name "*.conf" | sort > conf_list.txt**


//COMMANDS TO NOTE
**find /etc -type f -name "*.conf"**
==> find, inside directory 'etc', type of file, with name like *.config

//NOTE
    grep -> searches by file content
    find -> searches by file name or properties (like file extension)
